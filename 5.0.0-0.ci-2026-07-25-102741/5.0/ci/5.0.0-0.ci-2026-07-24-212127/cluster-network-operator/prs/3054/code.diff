diff --git a/bindata/network/frr-k8s/config.yaml b/bindata/network/frr-k8s/config.yaml
index a7b64c7142..164ef2dc79 100644
--- a/bindata/network/frr-k8s/config.yaml
+++ b/bindata/network/frr-k8s/config.yaml
@@ -69,13 +69,15 @@ data:
     #frr_profile="traditional"
     #frr_profile="datacenter"
 
-    #
     # This is the maximum number of FD's that will be available.
     # Upon startup this is read by the control files and ulimit
     # is called. Uncomment and use a reasonable value for your
     # setup if you are expecting a large number of peers in
     # say BGP.
-    #MAX_FDS=1024
+    # BFD opens multiple sockets per VRF. Bump this to a reasonable
+    # value as the default is 1024 which is too low for high scale 
+    # production networks.
+    MAX_FDS=65536
 
     # The list of daemons to watch is automatically generated by the init script.
     #watchfrr_options=""
