diff --git a/.cursor/rules/comments.mdc b/.cursor/rules/comments.mdc
new file mode 100644
index 0000000000..5d23c445df
--- /dev/null
+++ b/.cursor/rules/comments.mdc
@@ -0,0 +1,98 @@
+---
+description:
+  When to comment (why not what); self-documenting code first; generic examples only; complements
+  code-style and variable-naming.
+globs:
+  - apps/**/*.ts
+  - apps/**/*.tsx
+  - libs/**/*.ts
+  - libs/**/*.tsx
+alwaysApply: false
+---
+
+# Comments
+
+Write **self-documenting** code first (clear names, types, structure, small helpers). Add comments **sparingly**, only for **non-obvious _why_** (product rule, version quirk, surprising data shape)—not to restate **_what_** the code does.
+
+Lint/format and `yarn fix-code-style`: **code-style**. Naming: **variable-naming**. This file covers comment **use** only.
+
+Avoid comment text that only points at other files or line numbers—it goes stale. Prefer a short *why* in place. (Examples below stay generic.)
+
+## Default
+
+Omit the comment when control flow and names are enough. Prefer a rename or a small helper over a long explanation.
+
+## When to add a comment
+
+- A **product or UX rule** the code does not make obvious (e.g. show a notice only when there is no hard failure).
+- A **surprising input or invariant** so the next reader does not "fix" it incorrectly (e.g. several values in one comma-separated string). Explaining **input shape** next to a cast is *why*, not cast-justification—see **Hygiene**.
+
+## When not to add a comment
+
+Do not narrate each `if` / `else` branch. Prefer clearer names. If one idea is hard (e.g. a feature flag), use **one** short *why* on the **outer** branch instead of a comment on every branch.
+
+**Principle:** comments express **why** (existence, order, edge case)—not **what**.
+
+## Hygiene
+
+When changing **non-generated** files, keep comments accurate—update or remove comments that no longer match behavior.
+
+Do **not** add comments whose main purpose is to justify `as` / `!`. Do **not** leave blocks of commented-out code (use version control). Do **not** add file- or module-level "this file does..." banners. A short *why* next to a cast is fine when it describes real **data shape**, not the cast itself.
+
+Generated/synced paths are out of scope for edits here—see **generated-sdks-and-types**.
+
+## Tests, JSDoc, style
+
+Same comment bar as production code. Do not document that `before` / `beforeEach` is "setup."
+
+Do **not** add JSDoc unless the task explicitly calls for public API documentation.
+
+Prefer `//` for line comments. In JSX, follow ESLint (`{/* ... */}` where required). Keep comments in **English**.
+
+## Issue URLs in comments
+
+Avoid Jira/GitHub URLs in source—track work in the issue tracker; inline links go stale.
+
+## Do not
+
+- Add `TODO`, `FIXME`, or `HACK` markers (reserve those for human authors when appropriate).
+- Add `eslint-disable-*` comments—fix the lint issue or leave the code unchanged within the task scope.
+- Edit **generated or synced** code or add narrative comments there—follow **generated-sdks-and-types**.
+
+## Snippets (illustrative only)
+
+**Good** — product rule not obvious from JSX alone:
+
+```tsx
+<>
+  {/* Only show pending notice when nothing is in a hard-failure state */}
+  {!failedItems.length && <PendingItemsNotice items={pendingItems} />}
+</>
+```
+
+**Good** — surprising input shape (*why* for `split`, not cast-justification):
+
+```typescript
+function valuesAreAllowed(value: unknown): boolean {
+  if (!value) return true;
+  // Field can carry multiple values in one comma-separated string
+  const entries = (value as string).split(',');
+  return entries.every(isAllowedEntry);
+}
+```
+
+**Avoid** — comments that only narrate branches the code already expresses (noise, easy to drift):
+
+```tsx
+const options = React.useMemo(() => {
+  if (flag) {
+    // When flag is on...
+    if (a) {
+      // If a, return B
+      return B;
+    }
+    return C;
+  }
+  return D;
+}, [flag, a, B, C, D]);
+```
diff --git a/.cursor/rules/variable-naming.mdc b/.cursor/rules/variable-naming.mdc
new file mode 100644
index 0000000000..8d8a13d671
--- /dev/null
+++ b/.cursor/rules/variable-naming.mdc
@@ -0,0 +1,74 @@
+---
+description:
+  TS/TSX naming in apps/libs—casing, React, Redux, forms, length, single-letter conventions;
+  complements code-style and translations rules.
+globs:
+  - apps/**/*.ts
+  - apps/**/*.tsx
+  - libs/**/*.ts
+  - libs/**/*.tsx
+alwaysApply: false
+---
+
+# Variable naming
+
+Do **not** restate ESLint/Prettier or `yarn fix-code-style` (**code-style**). Do **not** restate the
+`t('ai:…')` workflow (**translations**).
+
+**Precedence:** If a bullet disagrees with the file you edit, **match the file** (**Consistency**
+below). Defaults for new code only—no drive-by renames.
+
+## Casing
+
+`camelCase` for values/functions; `PascalCase` for components, context providers, enums;
+`UPPER_SNAKE_CASE` for stable module constants. **`Id`** in camelCase (`clusterId`), not `ID`.
+
+## React
+
+Hooks `use` + `PascalCase`; handlers `handle*`; callback props `on*`. Refs `*Ref`; file-local
+subcomponents `PascalCase`. Prefer `handleSubmit`, `onChange`, `isModalOpen` over repeating the
+screen in every name; add a qualifier only to disambiguate (`handleSaveDraft` vs `handlePublish`,
+`handleClusterCreate` vs `handleClusterUpdate`). `useState`: `[foo, setFoo]`. `isLoading` or
+`loading` OK; narrow (e.g. `isInfraEnvLoading`) only when **several** loading flags share a scope.
+
+## Booleans
+
+Prefer `is*`, `has*`, `can*` (`has*` ~ possession/completion, `is*` ~ simple flags). Prefer positive
+names; use negative / `isNot*` when the call site reads clearer.
+
+## Redux / async
+
+Match the package (`select*`, `*Async` thunks, existing service names)—no parallel scheme.
+
+## Forms and types
+
+`*Props` (type or interface—do not force one); `*Values` / `*FormValues`. In **`libs/ui-lib/`**,
+suffix `Type` / `Values` / `Props` to avoid clashes; elsewhere mirror that package. Schemas:
+`*ValidationSchema`, `*Schema`, `get*InitialValues` like existing modules. i18n: **`t`** for
+`TFunction` in schema/label factories (see **Single letters**).
+
+## Length
+
+Short domain names (`clusterRows`); plural collections (`hosts`); drop redundant
+`List`/`Array`/`Items` when the type implies a collection. Drop scope-duplicated words
+(`CreateClusterForm` → `handleSubmit`). Reuse codebase abbreviations (SNO, OCM, VIP); no new opaque
+ones. Long logic → named `const` or helper; exported names may be longer, **locals** shorter.
+
+## Consistency
+
+Match dominant style in the file (including hook acronym casing).
+
+## Tests
+
+`create*` + entity (e.g. `createCluster`).
+
+## Single letters
+
+OK: index `i`/`j` with named elements `(row, i)`, `(col, j)`; ignored `_` with index; **`t`** for
+`TFunction` in factories; **`e`** in `catch`; generics `T`, `R`, `S`, `K`, `V`, … **Not** for domain
+entities—use words; if the file differs, **Consistency** wins.
+
+## Out of scope
+
+No repo-wide renames—**match local** spelling (e.g. `DropDown` vs `Dropdown`). No changing of
+existing variable names unless their meaning changes.
