diff --git a/pkg/infrastructure/baremetal/bootstrap.go b/pkg/infrastructure/baremetal/bootstrap.go
index 2f4186b00a3..769e93a6a37 100644
--- a/pkg/infrastructure/baremetal/bootstrap.go
+++ b/pkg/infrastructure/baremetal/bootstrap.go
@@ -48,6 +48,12 @@ func newDomain(name string) libvirtxml.Domain {
 					},
 				},
 			},
+			Controllers: []libvirtxml.DomainController{
+				{
+					Type:  "scsi",
+					Model: "virtio-scsi",
+				},
+			},
 		},
 		Features: &libvirtxml.DomainFeatureList{
 			PAE:  &libvirtxml.DomainFeature{},
@@ -392,7 +398,7 @@ func createBootstrapDomain(virConn *libvirt.Libvirt, config baremetalConfig, poo
 	liveCD := libvirtxml.DomainDisk{
 		Device: "cdrom",
 		Target: &libvirtxml.DomainDiskTarget{
-			Bus: "sata",
+			Bus: "scsi",
 			Dev: "sda",
 		},
 		Driver: &libvirtxml.DomainDiskDriver{
@@ -409,6 +415,11 @@ func createBootstrapDomain(virConn *libvirt.Libvirt, config baremetalConfig, poo
 			Order: 1,
 		},
 	}
+	if arch == "x86_64" {
+		// x86 traditionally uses IDE or SATA (only the latter is supported
+		// with the q35 machine type) for cdrom devices
+		liveCD.Target.Bus = "sata"
+	}
 
 	scratchDisk := libvirtxml.DomainDisk{
 		Device: "disk",
